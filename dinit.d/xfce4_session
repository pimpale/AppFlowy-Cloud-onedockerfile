type=process
command=sudo -E -u ubuntu bash -c "export HOME=/home/ubuntu && sleep 50 && cd /home/ubuntu && xfce4-session"
logfile=/var/log/dinit/xfce4-session.log
restart = true
# Requires X server
depends-on = xvfb